# 设计文档：.NET 10 运行时引导器（Bootstrap）

> 状态：**仅设计，未实现**（v1.22 维持框架依赖发布，现状不变）
> 日期：2026-09-19 · 作者：CPQ 系统优化工具项目

## 1. 背景与问题

主程序为**框架依赖**单文件发布（`-p:SelfContained=false`），目标机必须先安装
**.NET 10 Desktop Runtime（x64）** 才能运行。当前（v1.22 已发布）行为：

- 缺运行时：双击 exe → .NET 宿主弹系统对话框「需要 Microsoft .NET 10.0」→
  点「下载」只是**打开浏览器到官方下载页**，下载/安装均需手动完成；
- 应用内一切自动恢复机制（WebView2Loader 拉回等）**以程序已启动为前提**，
  对 .NET 运行时本身无效（鸡生蛋）。

目标：新增一个**原生小引导器**，检测到缺 .NET 10 时给出两个选择——
「自动下载安装」或「自行去官方下载页」——自动链路全程可验证、失败可回退。

**边界（硬约束）**：
- 主程序、R2 主程序对象、`version.json` 自动更新流 **一律不动**；
- 引导器 + 主程序**以 zip 并排捆绑分发**（免运行环境版）：主程序随 zip
  一次到位，引导器只负责「检测/安装运行时」一件事，装完拉起**同目录**主程序；
  不存在「引导器内再下主程序」的嵌套下载层；
- 引导器用 **C# .NET Framework 4.8** 写（~100–300KB 单 exe）：
  .NET Framework 4.8 是 Win10 1809+/Win11 **内置**组件（本项目支持线
  Win10 1903+ 起 4.8 恒在），任何目标机零前置即可启动引导器；
  铁律只有一条——**不 target .NET 10 自身**（那是要检测/安装的对象，
  用 .NET 10 写引导器在缺运行时机器上起不来，鸡生蛋）。

## 2. .NET 10 检测

主判据（注册表，无需任何运行时就绪假设）：

- `HKLM\SOFTWARE\dotnet\setup\amd64\HostFxR`（x86 对应 `x86` 键）
  下枚举 `FxVersion*` 值，存在 `10.x` 即视为已装基础运行时；
- **补充判据**：`%ProgramFiles%\dotnet\shared\Microsoft.WindowsDesktop.App\`
  下存在 `10.*` 目录 —— **WPF 应用实际要求的是 WindowsDesktop 运行时**，
  只有基础 Runtime 而没有 Desktop Runtime 时程序照样打不开，
  所以判据必须以 **WindowsDesktop.App 10.x 目录存在**为准，注册表 HostFxR 仅作交叉核对。

结论为「已满足」时：引导器**直接拉起主程序后自退**（透传行为，零打扰）。

## 3. 双选项交互（UX）

检测到缺少 WindowsDesktop Runtime 10.x 时，弹模态对话框（原生对话框，无 UI 框架依赖）：

```
此工具需要 .NET 10 桌面运行时（本机未安装）
- 自动下载安装：下载微软官方安装包 → 校验 → 静默安装（需管理员）→ 自动打开工具
- 官方下载页：在浏览器中打开官方页面，自行下载安装后运行本工具

[ 自动下载安装（推荐） ]    [ 官方下载页 ]    [ 取消 ]
```

- 「官方下载页」：`ShellExecute` 打开
  `https://dotnet.microsoft.com/download/dotnet/10.0`，然后退出；
- 「取消」：退出；
- 自动链路中**任何一步失败** → 对话框降级为同文案 + 失败原因（可勾选「查看详情」
  展开错误文本框），并附「官方下载页」按钮 —— 手动路线永远是兜底。

## 4. 自动链路（逐步规格，自动链只下载运行时一份）

| 步 | 动作 | 失败处理 |
|---|---|---|
| ① 解析版本 | 常量 `RUNTIME_VERSION=10.0.x`（钉死具体补丁号，不追"latest"） | 常量缺失/格式错 → 直接走兜底 |
| ② 下载 | 先 **R2 镜像** `dl.cab.dpdns.org/net-runtime/dotnet-runtime-<v>-win-x64.exe`；超时/404 回落**微软官方直链**（构建时从官方下载页解析后写死为常量，不在线解析页面） | 双源均失败 → 兜底 |
| ③ 校验 | **SHA256 钉死常量**（同 MAS 脚本钉哈希的既有套路；常量来源与更新流程写入代码注释） | 不一致 → 删除临时文件 + 兜底（这是防劫持的一票否决项） |
| ④ 安装 | 提权（`ShellExecuteEx runas`）执行 `installer /quiet /norestart`，`WaitForExit` 读退出码（0=成功，7402=已装更新版本亦按成功放行） | 非 0 → 兜底，回显真实退出码 |
| ⑤ 验证 | 复查 §2 判据（WindowsDesktop.App 10.x 目录出现） | 判据仍不满足 → 兜底（不谎报成功） |
| ⑥ 启动 | 拉起**同目录主程序**（zip 并排捆绑，见 §6）；引导器自退。同目录主程序缺失 → 提示「主程序文件缺失，请重新下载免运行环境版」并附官方下载页链接，**不增加下载层** | 拉起失败 → 兜底 |

临时文件统一落 `%TEMP%\cpq-bootstrap\`，链路结束（含失败）即清理。
自动链**只下载运行时一份**（~60MB）；主程序不在此下载（随 zip 已到位）。

**多线程下载：本期不做**。安装包 50–70MB，单连接通常可接受；R2 源本身已具备
"更快"的收益。留扩展位：② 步接口按「可切换单连接/分块（Range）」设计，后续
移植主程序 bezzad 分块下载逻辑即可，不改调用方。

**速度事实（对「比官网快吗」的回答）**：官网下载页只是网页，实际都走微软
CDN；引导器下同一安装包 = 同源，裸速度不更快。真实收益点有二：
a) R2 镜像源（Cloudflare 边缘，零出口流量费）可能比微软 CDN 顺手，双源策略
   先试 R2；b) 省掉「开浏览器→找文件→手动安装」的人工环节。

## 5. 安全与诚实性

- 钉哈希 + 官方 Authenticode 签名（可选增强：WinVerifyTrust，主程序已有
  `AuthenticodeVerifier` 的同款 P/Invoke 可参照）；
- 微软官方直链常量在注释中注明来源 URL 与**每次运行时补丁升级后必须同步更新
  引导器常量**的维护流程（与 MAS 钉哈希同口径）；
- 兜底文案与实现一致：所有「失败原因」展示真实错误码/HTTP 状态，不写「已安装」
  之类的虚假成功。

## 6. 分发形态

| 物 | 变化 |
|---|---|
| R2 对象 | 新增 2 个对象：`免运行环境版 zip`（`系统清理与优化工具_vX.0_免运行环境版.zip`，
   含 引导器.exe + 主程序.exe 并排，~10.5MB，主程序 exe 近乎不可压缩）
   + `net-runtime/dotnet-runtime-10.0.x-win-x64.exe`（运行时镜像，R2 零出口流量费）；
   现有主程序对象、台账结构不变（台账加 2 条） |
| 官网下载页 | 两个入口：「免运行环境版（推荐，首次使用）」= zip 直链；
   「普通版（已有 .NET 10 运行时）」= 现主程序直链（维持不变，高级用户/
   已装运行时的用户走这条，下载最小）。措辞不出现 R2/技术词 |
| 用户旅程 | **有 .NET 10**：解压 zip → 双击主程序直接跑（引导器为备用件），
   全程 0 下载。
   **缺 .NET 10**：解压 → 双击引导器 → 双选项 → 自动链只下载运行时一份（~60MB）
   → 装完拉起同目录主程序。一次自动化下载，无嵌套 |
| 程序内自动更新 | **不变**（仍走 version.json → 主程序对象）；引导器只在
   「首装/换机」场景出现一次，已装运行时的机器上主程序后续升级与引导器无关 |
| GitHub Release | 同 R2：新增 zip 资产（英文命名）+ 运行时镜像不入 GitHub（R2 专属）；
   主程序资产不变 |

## 7. 验收清单（实现时执行）

1. 无 .NET 10 的 Win11 虚拟机：解压 zip → 双击引导器 → 双选项出现；
   「官方下载页」打开浏览器正确 URL；「取消」退出码 0；
   **自动链全程只出现一次下载**（运行时），装完拉起同目录主程序且可正常启动；
2. 失败回退：断网模拟（② 失败）、篡改镜像 SHA（③ 失败）、UAC 拒绝（④ 失败）
   三种场景均落到兜底对话框且文案与真实原因一致；
   删掉同目录主程序再跑 → 出现「主程序缺失，请重新下载」提示（不出现下载层）；
3. 已装 .NET 10 的机器：解压 zip → 双击**主程序**直接启动（引导器根本不参与）；
   双击引导器 → 无弹窗、<2s 透传拉起主程序；
4. 主程序自动更新流回归：version.json 下载/替换行为不受本设计影响（抽验一次）；
5. 引导器体积 ≤500KB、.NET Framework 4.8 单文件、x64 主架构（x86 视需求）；
   在干净 Win10 1903 VM 上确认引导器零前置可启动。

## 8. 版本计划

- 本设计合入仓库即生效；**实现放在 v1.23**（届时若 .NET 10 补丁号升级，
  §4 ①③ 常量同步更新）；v1.22 及以前已发布物**零改动**。
- 设计修订记录：2026-09-19 ② 形态定案——引导器语言改 .NET Framework 4.8
  （Win10/11 内置，无需原生 C++）；分发改「zip 并排捆绑」（主程序随包一次
  到位，自动链只下载运行时，消除嵌套下载层）。
