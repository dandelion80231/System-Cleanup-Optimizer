# 设计文档：.NET 10 运行时引导器（Bootstrap）

> 状态：**仅设计，未实现**（v1.22 维持框架依赖发布，现状不变）
> 日期：2026-09-19 · 作者：CPQ 系统优化工具项目

## 1. 背景与问题

主程序为**框架依赖**单文件发布（`-p:SelfContained=false`），目标机必须先安装
**.NET 10 Desktop Runtime（x64）** 才能运行。当前（v1.22 已发布）行为：

- 缺运行时：双击 exe → .NET 宿主弹系统对话框「需要 Microsoft .NET 10.0」→
  点「下载」只是**打开浏览器到官方下载页**，下载/安装均需手动完成；
- 应用内一切自动恢复机制（WebView2Loader 拉回等）**以程序已启动为前提**，
  对 .NET 运行时本身无效（鸡生蛋）。

目标：新增一个**原生小引导器**，检测到缺 .NET 10 时给出两个选择——
「自动下载安装」或「自行去官方下载页」——自动链路全程可验证、失败可回退。

**边界（硬约束）**：
- **单一分发文件**：对外仍是一个 exe（沿用主程序文件名，无 zip、无双入口、
  无「免运行环境版/普通版」双按钮）。该 exe = **NF4.8 外壳引导器 + 内嵌
  .NET 10 主程序**（主程序作嵌入资源，构建时钉 SHA256）；
- 每版本 R2 **只新增 1 个对象**（智能外壳）；另设 **1 个跨版本永久的运行时
  镜像对象**（.NET 10 运行时安装包，全版本共用；10.x 补丁升级才动一次）；
- 外壳用 **C# .NET Framework 4.8** 写：.NET Framework 4.8 是 Win10 1809+/
  Win11 **内置**组件（本项目支持线 Win10 1903+ 起恒在），缺 .NET 10 的机器
  也能启动外壳；铁律只有一条——**不 target .NET 10 自身**（那是要检测/
  安装的对象，用 .NET 10 写外壳在缺运行时机器上起不来，鸡生蛋）。

## 2. .NET 10 检测

主判据（注册表，无需任何运行时就绪假设）：

- `HKLM\SOFTWARE\dotnet\setup\amd64\HostFxR`（x86 对应 `x86` 键）
  下枚举 `FxVersion*` 值，存在 `10.x` 即视为已装基础运行时；
- **补充判据**：`%ProgramFiles%\dotnet\shared\Microsoft.WindowsDesktop.App\`
  下存在 `10.*` 目录 —— **WPF 应用实际要求的是 WindowsDesktop 运行时**，
  只有基础 Runtime 而没有 Desktop Runtime 时程序照样打不开，
  所以判据必须以 **WindowsDesktop.App 10.x 目录存在**为准，注册表 HostFxR 仅作交叉核对。

结论为「已满足」时：外壳把内嵌主程序解到缓存目录（见 §4）后直接启动，
不再走任何下载分支（零打扰、零流量）。

## 3. 双选项交互（UX）

检测到缺少 WindowsDesktop Runtime 10.x 时，弹模态对话框（原生对话框，无 UI 框架依赖）：

```
此工具需要 .NET 10 桌面运行时（本机未安装）
- 自动下载安装：下载微软官方安装包 → 校验 → 静默安装（需管理员）→ 自动打开工具
- 官方下载页：在浏览器中打开官方页面，自行下载安装后运行本工具

[ 自动下载安装（推荐） ]    [ 官方下载页 ]    [ 取消 ]
```

- 「官方下载页」：`ShellExecute` 打开
  `https://dotnet.microsoft.com/download/dotnet/10.0`，然后退出；
- 「取消」：退出；
- 自动链路中**任何一步失败** → 对话框降级为同文案 + 失败原因（可勾选「查看详情」
  展开错误文本框），并附「官方下载页」按钮 —— 手动路线永远是兜底。

## 4. 启动/安装链路（逐步规格）

| 步 | 动作 | 失败处理 |
|---|---|---|
| ① 解析版本 | 常量 `RUNTIME_VERSION=10.0.12`（钉死，不追"latest"；2026-09-19 时 10.0.x 最新补丁为 10.0.12） | 常量缺失/格式错 → 直接走兜底 |
| ② 下载 | 先 **R2 镜像** `dl.cab.dpdns.org/net-runtime/windowsdesktop-runtime-10.0.12-win-x64.exe`（**已上传，SHA 见下**）；
   超时/404 回落**微软官方直链常量** `https://builds.dotnet.microsoft.com/dotnet/WindowsDesktop/10.0.12/windowsdesktop-runtime-10.0.12-win-x64.exe`（不在线解析页面） | 双源均失败 → 兜底 |
| ③ 校验 | **SHA256 钉死常量**（同 MAS 脚本钉哈希的既有套路；常量来源与更新流程写入代码注释） | 不一致 → 删除临时文件 + 兜底（这是防劫持的一票否决项） |
| ④ 安装 | 提权（`ShellExecuteEx runas`）执行 `installer /quiet /norestart`，`WaitForExit` 读退出码（0=成功，7402=已装更新版本亦按成功放行） | 非 0 → 兜底，回显真实退出码 |
| ⑤ 验证 | 复查 §2 判据（WindowsDesktop.App 10.x 目录出现） | 判据仍不满足 → 兜底（不谎报成功） |
| ⑥ 启动 | 解出内嵌主程序（嵌入资源，构建时钉 SHA256）到
   `%LOCALAPPDATA%\CpqSystemTool\cache\`（缓存目录；已存在且 SHA 一致则跳过解压），
   启动主程序 → 外壳自退。缺 .NET 10 的机器先走 §3 自动链装运行时再回这里；
   有 .NET 10 的机器首启解压一次（~10MB，秒级），之后直走缓存 |
   解压/校验失败 → 兜底（写明真实错误，不谎报） |

**自动链只下载运行时一份**（~60MB，R2 镜像）；主程序随外壳分发，
下载层**零嵌套**。

**体积与流量事实**：智能外壳 ≈ 主程序体积 + ~0.2–0.3MB（外壳代码）；
有 .NET 10 的机器零额外下载；缺 .NET 10 的机器一次性 +运行时 ~60MB。

**多线程下载：本期不做**。安装包 50–70MB，单连接通常可接受；R2 源本身已具备
"更快"的收益。留扩展位：② 步接口按「可切换单连接/分块（Range）」设计，后续
移植主程序 bezzad 分块下载逻辑即可，不改调用方。

**速度事实（对「比官网快吗」的回答）**：官网下载页只是网页，实际都走微软
CDN；引导器下同一安装包 = 同源，裸速度不更快。真实收益点有二：
a) R2 镜像源（Cloudflare 边缘，零出口流量费）可能比微软 CDN 顺手，双源策略
   先试 R2；b) 省掉「开浏览器→找文件→手动安装」的人工环节。

## 5. 安全与诚实性

- 钉哈希 + 官方 Authenticode 签名（可选增强：WinVerifyTrust，主程序已有
  `AuthenticodeVerifier` 的同款 P/Invoke 可参照）；
- 微软官方直链常量在注释中注明来源 URL 与**每次运行时补丁升级后必须同步更新
  引导器常量**的维护流程（与 MAS 钉哈希同口径）；
- 兜底文案与实现一致：所有「失败原因」展示真实错误码/HTTP 状态，不写「已安装」
  之类的虚假成功。

## 6. 分发形态

| 物 | 变化 |
|---|---|
| R2 对象 | 新增 2 个对象：`免运行环境版 zip`（`系统清理与优化工具_vX.0_免运行环境版.zip`，
   含 引导器.exe + 主程序.exe 并排，~10.5MB，主程序 exe 近乎不可压缩）
   + `net-runtime/dotnet-runtime-10.0.x-win-x64.exe`（运行时镜像，R2 零出口流量费）；
   现有主程序对象、台账结构不变（台账加 2 条） |
| 官网下载页 | 两个入口：「免运行环境版（推荐，首次使用）」= zip 直链；
   「普通版（已有 .NET 10 运行时）」= 现主程序直链（维持不变，高级用户/
   已装运行时的用户走这条，下载最小）。措辞不出现 R2/技术词 |
| 用户旅程 | **有 .NET 10**：解压 zip → 双击主程序直接跑（引导器为备用件），
   全程 0 下载。
   **缺 .NET 10**：解压 → 双击引导器 → 双选项 → 自动链只下载运行时一份（~60MB）
   → 装完拉起同目录主程序。一次自动化下载，无嵌套 |
| 程序内自动更新 | **不变**（仍走 version.json → 主程序对象）；引导器只在
   「首装/换机」场景出现一次，已装运行时的机器上主程序后续升级与引导器无关 |
| GitHub Release | 同 R2：新增 zip 资产（英文命名）+ 运行时镜像不入 GitHub（R2 专属）；
   主程序资产不变 |

## 7. 验收清单（实现时执行）

1. 无 .NET 10 的 Win11 虚拟机：双击智能外壳 → 双选项出现；「官方下载页」
   打开正确 URL；「取消」退出码 0；**自动链全程只出现一次下载**（运行时，
   R2 源），装完主程序被解压启动且可正常使用；
2. 失败回退：断网模拟（② 失败）、篡改 R2 运行时 SHA（③ 失败）、UAC 拒绝
   （④ 失败）三种场景均落到兜底对话框且文案与真实原因一致；
3. 已装 .NET 10 的机器：双击外壳 → **无弹窗零下载**，首启解压一次（秒级）
   后主程序正常启动；二启直走缓存 <2s；
4. 自动更新回归：version.json → 智能外壳替换旧版外壳的流程跑通一次
   （Updater 逻辑未改动，抽验即可）；
5. 体积/形态：外壳单文件 ≈ 主程序体积 +0.3MB 以内、.NET Framework 4.8、
   x64；干净 Win10 1903 VM 零前置可启动；
6. R2 台账：每版本 1 条 + 运行时 1 条（跨版本永久）；运行时对象 URL 进
   官网/外壳常量时做一次哈希核验。

## 8. 运行时镜像常量（2026-09-19 已上传 R2）

| 项 | 值 |
|---|---|
| 对象键（R2 桶 mvp） | `net-runtime/windowsdesktop-runtime-10.0.12-win-x64.exe` |
| 生产域 URL | `https://dl.cab.dpdns.org/net-runtime/windowsdesktop-runtime-10.0.12-win-x64.exe`（回读 SHA 一致） |
| 官方源 | `https://builds.dotnet.microsoft.com/dotnet/WindowsDesktop/10.0.12/windowsdesktop-runtime-10.0.12-win-x64.exe` |
| 大小 | 60,032,984 B |
| SHA256 | `55a67d8476cde95a9cc43a95803b4f54446e7c9251ed22aa6421d4908174ae84` |
| Authenticode | Valid（CN=.NET, O=Microsoft Corporation） |

**命名注意**：.NET 10 起 Windows 桌面运行时安装包改名为 `windowsdesktop-runtime-*`
（旧 `dotnet-desktop-runtime-*` 在 10.x 已不存在）；官方自托管源在
`/dotnet/WindowsDesktop/` 通道下，**本工具（WPF）必须装这个 Desktop 运行时**，
基础 `dotnet-runtime-*` 不满足。
**维护流程**：.NET 10 补丁升级（10.0.13+）时——重新从官方源下载 → SHA 变更 →
替换 R2 同键对象 + 更新本文档与外壳常量（同 §5 钉哈希维护口径）。

## 9. 版本计划

- 本设计合入仓库即生效；**实现放在 v1.23**（届时若 .NET 10 补丁号升级，
  §4 ①③ 与 §8 常量同步更新）；v1.22 及以前已发布物**零改动**。
- 2026-09-19：运行时镜像（10.0.12，60MB，微软官方签名）已先行上传 R2
  `net-runtime/` 前缀备用，外壳实现时直接引用 §8 常量。
- 设计修订记录：
  ① 2026-09-19：引导器语言改 .NET Framework 4.8（Win10/11 内置，无需原生 C++）；
  分发改「zip 并排捆绑」（自动链只下载运行时，消除嵌套下载层）；
  ② 2026-09-19（终案）：zip 双对象/双入口被否——每版本保留两份不好维护。
  定案为**单一智能外壳**（NF4.8 外壳 + 内嵌主程序）：每版本 R2 仅 1 对象，
  运行时镜像跨版本共用仅 1 对象；官网单一下载按钮；自动链只下运行时；
  官方下载页为第二选择。

## 9. 实测教训（2026-09-20，测试构建验证）

- **主程序 manifest = requireAdministrator**：外壳（普通权限）启动主程序必须用 ShellExecute（`UseShellExecute=true`），由系统弹 UAC；`CreateProcess`（`UseShellExecute=false`）会以 ELEVATION_REQUIRED（"请求的操作需要提升"）失败。
- 外壳本身**不**自提权；仅"自动安装"步骤用 runas 起安装器（UAC），装完外壳（仍普通权限）再 ShellExecute 主程序 → 用户共两次 UAC（安装器一次、主程序一次，主程序那次与双击桌面版一致）。
- 外壳右键以管理员运行时，主程序启动**不再**弹 UAC（同级直接放行）——用户反馈"必须右键管理员才能跑"即此差异，非提权丢失。
- 自动安装实测成功路径：R2 镜像下载 + SHA 校验 + `/quiet /norestore` 安装 10.0.12 → `RuntimePresent` 复查通过 → 直接拉主程序。
- 禁用/恢复 .NET 10 的测试 bat 必须**遍历所有 10.x 目录**（钉死单版本会在装上新补丁后失效：10.0.11 被禁用但 10.0.12 已装 → "无运行时"模拟不再成立）。
- 运行时目录被主程序占用时 rename 会失败（bat 内提示先关主程序）；`RecoverMovedDataRoot` 会随"数据跟随 exe"把数据迁到缓存目录（已知设计，v1.23 定稿时再处理锚点固定）。
