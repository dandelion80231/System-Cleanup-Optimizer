# 审查发现修复清单 — CpqSystemTool（v1.02 存量，待发布 v1.03）

日期：2026-08-11
依据：`code-review-triple-2026-08-11.md` 三遍审查。4 路并行子 Agent 改代码，主对话统一 `dotnet build -c Release` 验证 → 0 错误 0 警告。

---

## 一、实际代码改动

### 安全 / 信任边界
- `Modules/Activation.cs:265` — MAS 激活改用 `System.IO.Path.Combine(Environment.SystemDirectory, "WindowsPowerShell", "v1.0", "powershell.exe")` 完整路径 + `-EncodedCommand`，移除裸 `powershell.exe` 的 PATH 劫持风险；加注释说明依赖 HTTPS 信任、未做哈希钉扎。
- `Modules/ChocolateyResolver.cs:106-109` — OData 过滤 `id` 加白名单 `^[A-Za-z0-9.\-]+$`，非法 id 直接 `return false`。
- `Modules/OfficeInstall.cs:115-116` — `pid`/`channel` 拼 XML 前用 `System.Security.SecurityElement.Escape` 转义。
- `Modules/OfficeInstall.cs:88-93` / `Modules/EdgeCore.cs:109-117` — 下载后加 `File.Exists` + `Length > 0` 校验，损坏/空文件被拦截。

### 逻辑 / 控制流
- `MainWindow.Maint.cs:694` — `RefreshDepStatus` 空 `catch {}` 改为 `Debug.WriteLine` 记录 `IsNodeDepsReady` 异常，不再把"检测异常"误显为"Node 未安装"。
- `Modules/ProbeBrowserHost.cs:410` — 审查 agent 误报 `storeHit` 空引用（实为 `List<(string Href,string Title)>` 值元组，不会是 null，原 `!string.IsNullOrEmpty(storeHit.Href)` 已安全）；修复 agent 误加 `storeHit != null` 触发 CS0019 编译失败，**已还原**为 `!string.IsNullOrEmpty(storeHit.Href)`。

### 死代码清理 + 复用
- `MainWindow.Theme.cs:385/:150` — 删裸 `Debug.WriteLine` 状态跟踪 / pwsh7 通道失败日志各一处。
- `OtherTweaksDialog.cs:236-239` — 删孤立 `/// <summary>` 文档注释块（其后无方法体）。
- `OtherTweaksDialog.cs:190-196` — 内联 `taskkill explorer`+`Process.Start("explorer.exe")` 改为调用 `RegistryHelper.RestartExplorer(_ => { })`。
- `OtherTweaksDialog.cs` — 4 处注册表 Dword 读取模板改为 `RegistryHelper.GetDwordState(...)`。
- `CustomSoftwareDialog.cs:304` — 删冗余列宽赋值（`ColumnDefinitions[1].Width` 与上一行同值）。
- `Tier3ConfirmDialog.cs:267` — 勾选项大小求和改为索引对齐配对遍历 `_all.Zip(_boxes, (it,b) => b.IsChecked==true ? it.SizeMB : 0).Sum()`（原 `Enumerable.Range` 写法在子集非连续时会错位）。
- `DialogChrome`（位于 `CustomSoftwareDialog.cs`）— 新增 `ShowError(TextBlock,string)` 与 `BuildTitleBar(Window,...)` 静态方法；`CustomSoftwareEditDialog` / `CustomSoftwareManagerDialog` / `InstallPathDialog` 复用（移除内联 ControlTemplate/触发器与重复 ShowError）；`Tier3ConfirmDialog` 标题栏因差异过大（无圆角、danger 红标题、负边距关闭按钮）**保留原样**不强行提取。

---

## 二、审查误报澄清（经修复 agent 核实已在仓库落地，无需改动）

- `RunOnBrowserThread` 已有 `try { ... } catch (Exception ex) { tcs.TrySetException(ex); }`，非挂起风险。
- `WebClient` 已用 `WebClientWithTimeout` 子类 + `TimeoutMs=10000`，非无限超时。
- `RegistryHelper.RunCommand` 超时分支已 `p.Kill()`。
- `ProbeEngine.cs` 已有 `MAX_REDIRECTS = 10` 限制重定向。
- 搜索/扩展名判断的 `ToLower()` 已用 `ToLowerInvariant()`。
- `RegistryHelper.GetDwordState` 与 `Exec.KillIfTimeout` 已存在并替换 7 处调用。

> 说明：三遍审查的部分"发现"实为审查 agent 基于不完整阅读产生的误报。修复阶段逐文件核实后，仅落地真实缺陷（安全 4 处、Maint 异常吞掉、死代码/复用），并纠正了其中一处引入的编译错误。

---

## 三、构建验证

```
dotnet build CpqSystemTool.csproj -c Release --source https://api.nuget.org/v3/index.json
→ 0 个错误，0 个警告，生成 系统清理与优化工具.exe
```

## 四、下一步（待用户确认）

如需交付：bump 版本号 6 处至 v1.03（csproj / app.manifest / APP_VERSION / 关于页 / README / build.bat）+ exe 带版本号 → 重新构建部署 → `git tag v1.03` → 推送 → 建 GitHub Release 上传 exe。
